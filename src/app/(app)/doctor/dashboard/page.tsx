import StubPage from '@/components/ui/StubPage';
export const metadata = { title: 'Doctor Dashboard — FLUETAS' };
export default function DoctorDashboardPage() {
  return <StubPage title="Doctor Dashboard" emoji="👨‍⚕️" description="View your patient list, pending reviews, today's consultations, and manage clinical notes — all gated by patient consent scopes." phase="Phase 4" />;
}
