import StubPage from '@/components/ui/StubPage';
export const metadata = { title: 'Admin — FLUETAS' };
export default function AdminPage() {
  return <StubPage title="Admin Panel" emoji="🛡️" description="Doctor verification, audit log viewer, and system management — restricted to admin accounts only." phase="Phase 4" />;
}
