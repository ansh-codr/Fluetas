import AdminDashboardPage from './dashboard/page';

export const metadata = {
  title: 'Platform Administration — FLUETAS',
};

export default function AdminRootPage() {
  return <AdminDashboardPage />;
}
